---
import '../styles/global.css';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    
    <!-- Google Fonts: Outfit -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  </head>
  <body class="font-['Outfit'] text-slate-50 antialiased min-h-screen bg-slate-950 selection:bg-cyan-500/30 relative z-0">
    <!-- Dark, premium background with subtle radial gradient -->
    <div class="fixed inset-0 -z-10 bg-gradient-to-br from-slate-900 via-slate-950 to-black pointer-events-none"></div>
    <slot />
  </body>
</html>
